# デザインシステム

このサイトの「合格に必要な知識を、最短で・気持ちよく」読み進めるための視覚言語をまとめます。
新しいページや UI パーツを追加する際は、このドキュメントのトークンとパターンを優先利用してください。

## デザインの基本方針

| 軸 | 方針 |
| --- | --- |
| **目的** | 試験合格のための「学習装置」。装飾のための装飾は禁止 |
| **読みやすさ** | 1 行あたり 30〜45 文字 (日本語)、行間 1.8、長文は H2 で区切る |
| **集中度** | 余計な動きなし。アニメーションは hover focus 等の機能アクセントのみ |
| **記憶定着** | 重要箇所は色 + 形 + 反復 (sc-key/sc-trap/sc-answer) で多重符号化 |
| **モバイル** | 通勤中の片手スマホで読める。図はピンチインで読める SVG |

## カラートークン

`src/styles/custom.css` 内で CSS Custom Properties として定義。

### ライト/ダーク 共通

| トークン | 用途 |
| --- | --- |
| `--sc-accent` | プライマリ (リンク、フォーカス、見出しアクセント) |
| `--sc-accent-soft` | プライマリの薄い背景 (sc-key の背景) |
| `--sc-warn` | 注意・誤答・罠 (sc-trap) |
| `--sc-warn-soft` | warn の薄い背景 |
| `--sc-ok` | 正答・完了 |
| `--sc-muted` | 補足テキスト |
| `--sc-card` | カード背景 |
| `--sc-card-border` | カード境界 |

### コア配色 (ライト)

- 背景: 紙のような温かい白 `#fafaf7`
- テキスト本体: `#0e1217` (高コントラスト)
- アクセント: 青緑系 `#0d7a8a` (サイバー寄りで信頼感)
- 警告: 朱色 `#c14545`
- 成功: 深緑 `#3a7a3a`

### ダーク

- 背景: `#0e1217`
- テキスト: `#e6eaef`
- アクセント: `#5fc9d9` (彩度を上げる)
- 警告: `#ff7676`

## タイポグラフィ

| 要素 | 値 |
| --- | --- |
| 本文 | 16px / line-height 1.8 / `var(--sl-font)` |
| H1 | 32px / 700 / 字間 -0.01em |
| H2 | 24px / 700 / 上線 1px のアンダーライン |
| H3 | 19px / 600 |
| code | 14px / `JetBrains Mono` |

日本語フォント: Hiragino Sans → Yu Gothic UI → Meiryo の優先順 (端末互換性最大)

## コンポーネント (Markdown / MDX 内で使う型)

### 1. 重要ポイント `<div class="sc-key">`

合格に必須の知識ポイント。アクセント色のサイドバー + 🔑 アイコン。

### 2. 罠 `<div class="sc-trap">`

受験生がハマる誤答。警告色 + ⚠️ アイコン。

### 3. 解答ボックス `<details class="sc-answer">`

問題の解答を折りたたみ表示。ユーザが先に考えてから開く設計。

### 4. 出典 `:::caution[出典]`

過去問解説の冒頭。Starlight の標準 admonition を流用。

### 5. Mermaid 図 `<Mermaid code={...} />`

クライアントサイドで描画。テーマ切替に追従。

## レイアウト

- ホームは Starlight `template: splash` で大きな hero
- ドキュメントページは標準 sidebar + content + on-this-page
- max-width 内容領域 800px (日本語に最適化)
- 図は中央寄せ、横スクロール許容

## アイコン

絵文字を限定的に使用 (機能アイコンのみ):
- 🔑 重要ポイント
- ⚠️ 罠/警告
- 📋 採点ポイント
- 🛡️ 防御策
- 🎯 解答

それ以外の絵文字は不使用 (CLAUDE.md ルールに合わせ、装飾目的の絵文字を避ける)。

## アニメーション

| 動き | 用途 |
| --- | --- |
| hover で 100ms 色変化 | リンク、ボタン |
| 折りたたみ open/close 200ms | sc-answer |
| 図描画フェードイン 300ms | Mermaid (任意) |

それ以上は使わない。学習を妨げるため。

## 拡張ガイド

新しい UI パーツを足したくなったら:
1. **既存コンポーネントで表現できないか** を最初に検討
2. 必要なら `src/components/<Name>.astro` で実装
3. クラス名は `sc-<role>` 命名 (この DESIGN.md のトークンに従う)
4. このファイルにコンポーネントエントリを追加

## ロードマップ (将来の改善)

- [ ] 過去問解説ページに「進捗マーカー」(完了/復習中/未着手) を CSV ベースで管理
- [ ] 午前 II 演習の Quiz UI コンポーネント
- [ ] PWA 化 (オフライン読書)
- [ ] 暗記カード機能 (重要用語)
- [ ] 検索の日本語精度向上 (kuromoji 連携)
